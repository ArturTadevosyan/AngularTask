import { MaskTextDirective } from './mask-text.directive';

describe('MaskTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskTextDirective();
    expect(directive).toBeTruthy();
  });
});
